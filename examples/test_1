birth
	die;
	die;
	die;
	int a[10](0);
	real b[10](0);
	string s1("Max"), s2("Min");
	iterator i, j;
	while(a or b[10] and b*a + a > !(10 and a * (b + a))) {
		real b[10](0.0);
		string s1("Max"), s2("Min");
		while(b){ die; };
	};
	get(a[10, b], b[10]);
	put(a or b[10] and b*a + a > !(10 and a * (b + a)), b); 
	a[b[10], z] = s[901] = w = 10;
	i:(b[10], 9, 1) {
		real b[10](0.0);
		string s1("Max"), s2("Min");
		while(b){ die; };
	};
death
